# 任务一 · 关键发现与对抗式审查记录

本文件记录任务一从"调参"深挖到"定稿"的完整发现链、诚实数字、以及对抗式审查(自己攻击自己结论)的结果。核心结论:**用多seed集成,诚实鲁棒 F1 ≈ 0.92~0.935。**

## 评估口径:三套设置,别混

| | 设置 | 代码 | 结果 |
|---|---|---|---|
| ① 单一留出 | 200→留40张(`VAL_SIZE=40,VAL_SEED=0`),训160评40 | `validate.py` | 老的乐观 0.945(**高估**) |
| ② K折OOF | 200→4折,每张都被"没训过它的模型"预测一次,累加全200 | 集成实验 | **诚实 0.92~0.935** |
| ③ 提交 | 训全部200张 → 预测100张无标签测试 | `run_ensemble.py` | 本地无法算分,估计~0.93 |

100张测试图**无公开标签**,故所有分数只能在200张有标注图上算。①只评了40张(一次抽样,恰好抽到容易的:那40张仅3个误报、P=0.99),高估;②覆盖全200(含难图),更诚实。**别信①的0.945,信②的~0.93。**

## 核心发现链

1. **阈值sweep** 挖出:单模型误报(FP)方差极大 —— 同一批数据、换训练随机种子,某些噪声重的图触发"误报级联"(单图几十~几百假框)。全200 K折OOF:最坏数据划分 F1=0.786(FP574)、最好 0.9153(FP111)。
2. **根因**:200张小数据集 + 训练随机性;扫描/印刷噪声被高通差分读成小差异。噪声重的图(如训练集 043)在不同 seed 下 FP 从 9 到 411(45×)。
3. **对症解 = 多seed集成**(N个不同 seed 的模型热图取平均):一个假框要多个模型同时幻觉才存活,而级联是模型专属的→被平均掉。实测:

| | 单模型 | 3-seed 集成 |
|---|---|---|
| seed0 好折 F1 | 0.9153 | **0.9350** |
| seed1 坏折 F1 | 0.7862 | **0.9220** |
| 跨折方差 | 0.129 | **0.013**(降10倍) |
| 坏折误报 | 574 | 30(砍95%) |

集成在好折也更优(不是以牺牲简单场景为代价),两折都赢。

## 对抗式审查(攻击自己的结论,5点全过)

1. **官方指标**:已核对确认 = 全局 F1(累加全测试集 TP/FP/FN 再算)= `evaluate.py` 算法。IoU 匹配阈值官方未公开,但多规则实证无关紧要:集成 OOF 在 中心点/IoU≥0.1/0.25/0.5 下 F1=0.935/0.935/0.934/0.922(仅±0.013),且每种规则集成都胜单模型。因 ~89% 漏报"真没检测到"(对任何规则都漏),规则撼不动大局,我们站保守侧。
2. **集成好折是否反亏**:否。好折 0.9153→0.9350,集成两折都赢。
3. **召回天花板**:真实。localization 分析显示 178 个漏报中 89% 是"真没检测到"(附近无框),非框尺寸问题;漏的中位 8px,是 U-Net 256→32 瓶颈的分辨率下限。三种召回杠杆(降阈值/正样本加权/Tversky)全部推不动召回(死钉~0.91)。
4. **方差框架修正**:"45×"是 init-seed 方差(部署 `seed=0` 定死、不吃);部署真正吃的是数据构成方差 ~5×(FP111 vs 574)。集成仍值,但对外说 5× 不是 45×。
5. **测试集健康检查**(100张真实测试图):对齐良好(位移中位0px、最大0.5px,phaseCorrelate响应中位0.896),变化信号亮占比中位1.27%/最大5.56%,每图框数中位7/最大11 —— **无对齐/透视灾难,无误报爆炸**。可视化抽查:框落在真实差异(加入的斜杠/图标/改动数字)上。

## 提交配置(run_ensemble.py)

- 训练 = 全部 200 张(不留出);模型 = 3 路不同 seed U-Net,热图平均集成 + TTA;阈值 = (0.3, 0.5)。
- 去噪/box0.7/正样本加权/Tversky 都**不进**(集成后无益或有害;详见发现链)。
- 复现:`python run_ensemble.py` → `outputs/submission_ens.csv`。

## 强 baseline 对比(证明赢点在表示不在骨干)

同一 4 通道输入 + 切片 + TTA 流程,只换网络(no-TTA 统一口径,留出 F1):我们 1.9M 4通道U-Net **0.920** ≈ U-Net++ R34(26M)0.918 > U-Net+R34预训练 0.900 > FC-Siam 0.806 > SegFormer 0.706 > DeepLabV3+ 0.609。**赢点在 4 通道对比表示 + 切片/TTA 流程,不在堆大骨干。** 见 `bench_baselines.py`。

## 后处理调参:确认 +0.033(2026-08,双 split K折 OOF,集成口径)

诊断出发点:基线两 split 差 0.049(split1 因误报级联掉到 0.886)——后处理阈值仍有余量。
做法:`eval_oof_sweep.py` 在 OOF 过程中**内联**评完整张 mask×box×min-area 网格
(不缓存热图——图最大 61MP,缓存过会撑爆磁盘,已踩坑),只落很小的计数 JSON;
`combine_sweep.py` 合并两 split 出对比。全网格与参考后处理逐配置自检一致(0 失配)。

| 配置 (mask\|box\|min-area) | F1均值 | split0 | split1 |
|---|---|---|---|
| 基线 0.3\|0.5\|scaled6 | 0.9101 | 0.9344 | 0.8858 |
| **采纳 0.3\|0.6\|abs4** | **0.9435** | 0.9549 | 0.9321 |
| 网格argmax 0.25\|0.65\|abs4 | 0.9500 | 0.9539 | 0.9461 |

- **采纳 (0.3, 0.6, min-area=4 绝对)**:+0.0334,**两个 split 都赢**;机理=提高 box 阈值滤掉集成后
  幸存的误报,split 间差距 0.049→0.008(正是我们第一痛点"误报方差"的对症解)。
- **网格 argmax 未采纳**:所有 top 配置都压在 box_thr=0.65 **扫描上边界**,有过拟合/过滤过头之嫌;
  取非边界、已确认的 0.60。(已另跑 box_thr 延伸到 0.85 的确认扫描,见下节。)
- **重要:调参必须在集成(部署)口径下判读。** 同一配置在**单模型**口径反而更差
  (0.825 < 0.857,split1 崩到 0.746/FP829)——单模型口径会给出相反结论。
- 提交:`MASK_THR=0.3 BOX_THR=0.6 MIN_AREA_MODE=abs4 python run_ensemble.py`
  → `submission_task1_tuned.csv`(662 框/100 图全覆盖/0 非法框/每图中位 7)。

## 参数最优性:box 已到内部峰、mask 不可优化(2026-08,延伸网格 + 连通域表)

第二轮把 box_thr 网格延伸到 0.85,并让 `eval_oof_sweep.py` **额外落"连通域表"**
(每候选框的 x,y,w,h,area,mean_prob,两 split 共 6.3 万个、仅 2MB)——
**此后任何 box_thr / min-area / 新后处理规则都能在表上秒算,不必再烧 GPU**(`fine_sweep.py`)。

- **box_thr:已解决,是干净的内部极值。** 0.01 步长精扫(mask 0.3, abs4, 两 split 均值):
  0.55→0.9505,**0.62–0.65 平台 0.9584–0.9587**,0.70→0.9538,0.75→0.9447。
  两轮独立运行都指向 0.62–0.66 → 采纳区间可靠;0.63 与 0.65 差 0.0003(噪声)。
- **min-area:惰性参数。** abs4 ≈ abs8 ≈ abs16 ≈ abs32(box 阈值已主导过滤)→ 保持 abs4 安全。
- **mask_thr:证明"不可优化",继续扩网格 = 拟合噪声。** 两轮独立运行给出**完全相反**的排序
  (box=0.65, abs4, 两split均值):

  | mask | run1 | run2 |
  |---|---|---|
  | 0.25 | **0.9500(最优)** | 0.9560 |
  | 0.30 | 0.9486 | 0.9584 |
  | 0.40 | 0.9479(最差) | **0.9603(最优)** |

  同一网格内 mask 的跨度(~0.005)远小于**跑间噪声(~0.02,cuDNN 非确定性 + 训练随机性)**
  → 取中值 0.30 是唯一稳健解。**方法论:参数排序在独立重跑间反转 = 该维度不可优化,别追。**
  脚本:`mask_cross.py`。
- **集成路数:已测,3→5 无增益(`eval_ens_size.py`,配对设计)。** 同一次 OOF 里训 5 个 seed、
  同时算 ens1..ens5(跨运行比 ens3/ens5 会被 ±0.02 跑间噪声淹没,等于白比),split0、9 档 box 均值:

  | 集成路数 | 1 | 2 | 3(部署) | 4 | 5 |
  |---|---|---|---|---|---|
  | F1 | 0.9333 | **0.9606** | 0.9545 | **0.9627** | 0.9601 |

  **曲线是锯齿形(2↑3↓4↑5↓),不是单调趋势** → 主导因素是"具体哪几个 seed 进了集成"(个体质量),
  不是集成规模。**1→2 路是唯一真跃迁(+0.027);2 路之后全在 0.955–0.963 的 ±0.004 带内晃。**
  挑 ens4 是从噪声里 cherry-pick(无法先验知道哪个 seed 强)。**结论:保持 ens3,T1 参数层到顶。**
  (理论上更多路方差更小,但本实验测不出均值差异,不值 +67% 训练开销。)

## 前沿论文迁移验证:Text-Aware SSIM(WACVW 2026)—— 证否,且原因可量化

**论文**:Jin et al., *Can You Find the Difference? Visually Identical Image Detection*, WACVW 2026(Amazon Prime Video)。
**它做什么**:判断两张图"相同/不同"的**二分类**(每对一个是非),不是定位。核心是
**OCR 找出文字区域 → 只在这些区域内做 SSIM 结构比对**,在 358 对影视海报上取得 F1=1.000。

**任务错配(第一层)**:它输出一个是非,我们要框出每一处并按 IoU≥0.5 算全局 F1;
它的差异是"整句标题被翻译/字幕整条替换"(语义级、大面积),我们是 **4–10px 微改动**;
它比的是两张**干净数字图**,我们是**数字模板 vs 印刷实物照片**(SSIM 会被印刷噪点/光照打爆)。

**但其中一个思想值得试**:"候选框不落在内容区域内 → 更可能是噪点",这**正好打在我们唯一剩下的空间上**
(oracle 分析:0.9435→0.971 的 +0.027 **全部锁在误报**;而误报"几乎全是凭空多框,落在页边噪点区")。
我们用**"内容/有墨区域"替代 OCR**(我们的差异含文字/图形/布局三类,OCR 只覆盖文字),零依赖。

**实测(全 200 张训练图,`probe_content_region.py` / `probe_mask_source.py`):**

| 掩码来源 | GT 覆盖率 | 面积占比 | 结论 |
|---|---|---|---|
| **模板端** | **41.6%** | 8.5% | ❌ 会误杀 **58%** 真改动 |
| 照片端 | 99.6% | 6.7% | ⚠️ 噪点本身就是照片墨迹,滤不掉 |
| 并集 | 99.6% | 8.7% | ⚠️ 同上 |

**两边都不成立**:能滤噪点的(模板端)会砍掉一大半召回;能保住召回的(照片端/并集)把噪点也算作内容。

**根因量化(健康度自检,排除"检测器失灵"):** 模板全图墨密度 0.081(检测器正常),
但 **GT 框内模板墨密度仅 0.087 ≈ 全图平均**,而 **GT 框内照片墨密度 0.427 ≈ 全图的 7.4 倍**。
→ **我们的差异绝大多数是"在照片里凭空加内容",改动位置的模板上本来什么都没有。**
而论文场景(海报文字被替换/翻译)**两侧都有文字**,所以它的 OCR 区域提议成立、我们这里不成立。

**这条负结果的价值**:它给出了**可迁移性失败的精确机理**,而不是"试了不行"。
同时顺带量出一个此前不知道的数据性质:**我们的改动以"新增"为主,而非"替换"**。


## 前沿零样本 baseline(2025-2026):证明本题必须"训练的高分辨率变化模型"

- **DINO 语义差分零样本(= AnyChange 的双时相潜匹配思想,在 DINOv2 patch 特征空间)= F1 0.006**
  (oracle 阈值,FP3360/FN1393)。原因:61MP 图必须降到 ~700px 才能喂 DINO,**4–10px 的
  微改动被降采样彻底抹掉**。→ 零样本基础模型(DINO/AnyChange 路线)在本题分辨率下无法定位微缺陷。
- SAM 零样本:权重下载损坏(国内网络),未得分;预期同样趴。
- 结论:有 200 张标注时,**训练 + 集成远胜零样本大模型**;我们 U-Net(OOF ~0.94)对 DINO-zs(0.006)
  是 150 倍差距。代码:`baseline_dino_zs.py`、`models/dino_diff.py`(`USE_DINO_DIFF=1` 可作第5通道)。
